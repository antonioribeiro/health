<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />

        <title>{{ config('health.title') }}</title>

        @yield('head')
    </head>

    <body>
        @yield('body')
    </body>
</html>
